import { makeStyles } from '@material-ui/core/styles';

export default makeStyles({
  graph: {
    display: 'flex'
  },
  graphHeader: {
    marginBottom: '2rem'
  },
  paper: {
    padding: '0.5rem'
  }
});